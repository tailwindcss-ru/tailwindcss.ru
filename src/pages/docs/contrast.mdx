---
title: "Контрастность"
description: "Утилиты для применения контрастных фильтров к элементу."
featureVersion: 'v2.1+'
---

import { Variants } from '@/components/Variants'
import { Disabling } from '@/components/Disabling'

export const classes = {
  plugin: function () {
    return function ({ addUtilities }) {
      addUtilities({
        '.contrast-0': { '--tw-contrast': 'contrast(0)' },
        '.contrast-50': { '--tw-contrast': 'contrast(.5)' },
        '.contrast-75': { '--tw-contrast': 'contrast(.75)' },
        '.contrast-100': { '--tw-contrast': 'contrast(1)' },
        '.contrast-125': { '--tw-contrast': 'contrast(1.25)' },
        '.contrast-150': { '--tw-contrast': 'contrast(1.5)' },
        '.contrast-200': { '--tw-contrast': 'contrast(2)' },
      })
    }
  }
}

## Применение

Используйте утилиты `contrast-{amount?}` вместе с утилитой `filter` для управления контрастностью элемента.

```html
<div class="filter contrast-125 ...">
  <!-- ... -->
</div>
```

## Адаптивность

Чтобы управлять контрастностью элемента в определенной контрольной точке, добавьте префикс `{screen}:` к любой существующей утилите контрастности. Например, используйте `md:contrast-150`, чтобы применить утилиту `contrast-150` только при средних размерах экрана и выше.

```html
<div class="filter contrast-110 **md:contrast-150** ...">
  <!-- ... -->
</div>
```

Для получения дополнительной информации о функциях адаптивного дизайна Tailwind ознакомьтесь с документацией [Адаптивного дизайна](/docs/responsive-design).

## Кастомизация

### Варианты

<Variants plugin="contrast" name="contrast" />

### Отключение

<Disabling plugin="contrast" name="contrast" />
