---
title: "Штрих"
description: "Утилиты для стилизации штриха элементов SVG."
---

import plugin from 'tailwindcss/lib/plugins/stroke'
import { Variants } from '@/components/Variants'
import { Disabling } from '@/components/Disabling'

export const classes = { plugin }

## Применение

Используйте `stroke-current`, чтобы установить цвет обводки SVG на текущий цвет текста. Это упрощает установку цвета обводки элемента, комбинируя этот класс с существующей [утилитой цвета текста](/docs/text-color).

Полезно для стилизации наборов иконок, таких как [Heroicons](http://heroicons.com/), которые полностью нарисованы штрихами.

```html purple
<template preview>
  <div class="text-center">
    <svg class="stroke-current text-purple-600 inline-block h-12 w-12" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <circle cx="8" cy="21" r="2" />
      <circle cx="20" cy="21" r="2" />
      <path d="M5.67 6H23l-1.68 8.39a2 2 0 0 1-2 1.61H8.75a2 2 0 0 1-2-1.74L5.23 2.74A2 2 0 0 0 3.25 1H1" />
    </svg>
  </div>
</template>

<svg class="**stroke-current text-purple-600** ..."></svg>
```

## Кастомизация

Управляйте тем, какие утилиты штрихов генерирует Tailwind, настроив раздел `theme.stroke` в вашем файле `tailwind.config.js`:

```diff-js
  // tailwind.config.js
  module.exports = {
    theme: {
-     stroke: {
-       current: 'currentColor',
-     }
+     stroke: theme => ({
+       'red': theme('colors.red.500'),
+       'green': theme('colors.green.500'),
+       'blue': theme('colors.blue.500'),
+     })
    }
  }
```

### Варианты

<Variants plugin="stroke" />

### Отключение

<Disabling plugin="stroke" />
