---
title: "Содержимое"
description: "Утилиты для управления содержимым псевдоэлементов до и после."
---

import { ArbitraryValues } from '@/components/ArbitraryValues'
import { BreakpointsAndMediaQueries } from '@/components/BreakpointsAndMediaQueries'
import { HoverFocusAndOtherStates } from '@/components/HoverFocusAndOtherStates'

export const classes = {
  utilities: {
    'content-none': { 'content': 'none' }
  }
}

## Основы использования

### Установка содержимого псевдоэлемента

Используйте утилиты `content-*` вместе с модификаторами вариантов `before` и `after`, чтобы установить содержимое псевдоэлементов `::before` и `::after`.

По умолчанию, `content-none` единственная доступная утилита с предварительно сконфигурированным содержимым. И хотя вы можете добавить дополнительные утилиты, [настроив свою тему](#customizing-your-theme), обычно имеет смысл просто использовать произвольное значение.

Используйте обозначение квадратных скобок, чтобы определять любое произвольное значение содержимого на лету.

```html {{ example: { p: 'none' } }}
<div class="px-4 sm:px-0">
  <div class="p-8 w-full max-w-xl mx-auto bg-white shadow-lg text-slate-500 dark:bg-slate-800 dark:text-slate-400">
    Более высокое разрешение означает больше, чем просто более качественное изображение. На дисплее Retina 6K, <a href="https://www.apple.com/pro-display-xdr/" class="text-blue-600 dark:text-sky-400 font-medium after:content-['_↗'] after:text-sm after:font-bold" target="_blank">Pro Display XDR</a> обеспечивает почти на 40% больше места на экране, чем дисплей с разрешением 5K.
  </div>
</div>
```

```html
Более высокое разрешение означает больше, чем просто изображение лучшего качества. Благодаря дисплею Retina 6K, <a class="text-blue-600 **after:content-['_↗']** ..." dark-class="text-sky-400 **after:content-['_↗']** ..." href="https://www.
apple.com/pro-display-xdr/" target="_blank">Pro Display XDR</a> дает вам почти на 40 процентов больше площади экрана, чем дисплей 5K.
```

### Ссылка на значение атрибута

Эти утилиты контента даже поддерживают функции CSS, такие как функция `attr()`, которую вы можете использовать для ссылки на значение, хранящееся в атрибуте:

```html {{ example: { p: 'none' } }}
<div class="px-4 sm:px-0">
  <div class="font-semibold text-slate-900 p-8 w-full max-w-xl mx-auto bg-white shadow-lg flex items-center justify-center dark:bg-slate-800 dark:text-slate-200">
    <div before="Hello World" class="before:content-[attr(before)]"></div>
  </div>
</div>
```

```html
<div before="Hello World" class="before:content-[attr(before)]">
  <!-- ... -->
</div>
```

### Использование пробелов и подчеркиваний

Поскольку пробел обозначает конец класса в HTML, замените любые пробелы в произвольном значении подчеркиванием:

```html {{ example: { p: 'none' } }}
<div class="px-4 sm:px-0">
  <div class="font-semibold text-slate-900 p-8 w-full max-w-xl mx-auto bg-white shadow-lg flex items-center justify-center dark:bg-slate-800 dark:text-slate-200">
    <div class="before:content-['Hello_World']"></div>
  </div>
</div>
```

```html
<div class="before:content-['Hello_World']">
  <!-- ... -->
</div>
```

Если вам нужно включить фактический символ подчеркивания, вы можете сделать это, экранировав его с помощью обратной косой черты:

```html {{ example: { p: 'none' } }}
<div class="px-4 sm:px-0">
  <div class="font-semibold text-slate-900 p-8 w-full max-w-xl mx-auto bg-white shadow-lg flex items-center justify-center dark:bg-slate-800 dark:text-slate-200">
    <div class="before:content-['Hello\_World']"></div>
  </div>
</div>
```

```html
<div class="before:content-['Hello\_World']">
  <!-- ... -->
</div>
```

---

## <Heading ignore>Применяя условно</Heading>

### <Heading ignore>Наведение, фокус и другие состояния</Heading>

<HoverFocusAndOtherStates defaultClass="before:content-['Not_Hovering']" featuredClass="before:content-['Hovering']" />

### <Heading ignore>Контрольные точки и медиа-запросы</Heading>

<BreakpointsAndMediaQueries defaultClass="before:content-['Mobile']" featuredClass="before:content-['Desktop']" />

---

## Использование пользовательских значений

### Настройка вашей темы

По умолчанию Tailwind предоставляет только утилиту `content-none`. Вы можете настроить эти значения, отредактировав `theme.content` или `theme.extend.content` в вашем файле `tailwind.config.js`.

```diff-js {{ filename: 'tailwind.config.js' }}
  module.exports = {
    theme: {
      extend: {
+       content: {
+         'link': 'url("/icons/link.svg")',
+       },
      }
    }
  }
```

Дополнительные сведения о настройке темы по умолчанию смотрите в документации [настройка темы](/docs/theme#customizing-the-default-theme).

### Произвольные значения

<ArbitraryValues property="content" featuredClass="before:content-['Hello_World']" />
