---
title: "Установите Tailwind CSS с Rails"
description: "Настройка Tailwind CSS в проекте Rails."
tool: Rails
---

<!--
#####
Тест в день запуска: работает с обычным пакетом tailwindcss 🎉
#####
-->

# Использование Tailwind CSS с Rails

## Создание вашего проекта

Начните с создания нового проекта Rails, если у вас его еще нет. Наиболее распространенный подход - использовать [Rails CLI](https://guides.rubyonrails.org/command_line.html#rails-new):

```shell
rails new my-project
```

## Настройка Tailwind CSS

Установите Tailwind с помощью `npm` или `yarn`:

```shell
yarn add tailwindcss
```

Затем добавим Tailwind в файл `postcss.config.js` нашего проекта:

```diff-js
  module.exports = {
    plugins: [
      require('postcss-import'),
+     require('tailwindcss'),
      require('postcss-flexbugs-fixes'),
      require('postcss-preset-env')({
        autoprefixer: {
          flexbox: 'no-2009'
        },
        stage: 3
      })
    ]
  }

```

Затем давайте создадим конфигурационный файл Tailwind по умолчанию:

```shell
npx tailwindcss init
```

В нашем проекте Rails создадим новый файл с именем `tailwind.css` внутри каталога `app/javascript/styles`. Этот файл будет содержать три декларатива `@tailwind`:

```css
@import "tailwindcss/base";
@import "tailwindcss/components";
@import "tailwindcss/utilities";
```

Затем давайте импортируем этот файл в пакет JavaScript нашего приложения (`app/javascript/packs/application.js`):

```diff-js
  require("@rails/ujs").start();
  require("turbolinks").start();
  require("@rails/activestorage").start();
  require("channels");
+ require("../styles/tailwind.css");

```

Наконец, в файле макета нашего приложения (`rails/app/views/layouts/application.html.erb`), давайте заменим `stylesheet_link_tag` на `stylesheet_pack_tag`:

```diff-html
  <html>
    <head>
      <title>RailsApp</title>
      <!-- ... -->

-     <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
+     <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
      <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    </head>

    <!-- ... -->
  </html>
```

Это оно! CSS Tailwind теперь доступен во всем приложении Rails 🎉
