---
title: "Установите Tailwind CSS с Angular"
description: "Настройка Tailwind CSS в проекте Angular."
tool: Angular
---

<!--
#####
Тест в день запуска: Angular работает с обычным пакетом tailwindcss 🎉
#####
-->

```preval installation
tool: Angular 
reference:
  name: Angular CLI
  link: https://angular.io/guide/setup-local#install-the-angular-cli
script: ng new 
```

```preval setup
version: latest 
dependencies:
  - postcss-loader
  - '@angular-builders/custom-webpack'
```


```preval configuration
postcss: false
purge: 
  - ./resources/**/*.blade.php
  - ./resources/**/*.js
  - ./resources/**/*.vue
```

Затем давайте создадим `webpack.config.js` в корне нашего проекта и скажем webpack обрабатывать файлы CSS с помощью `postcss-loader`:

```js
module.exports = {
  module: {
    rules: [
      {
        test: /\.css$/,
        loader: "postcss-loader",
        options: {
          postcssOptions: {
            ident: "postcss",
            plugins: () => [require("tailwindcss"), require("autoprefixer")],
          },
        },
      },
    ],
  },
}
```

```preval include
file: ./src/styles.css
```

Наконец, нам нужно обновить файл `angular.json`, чтобы он знал о нашей настраиваемой конфигурации веб-пакета. Нам нужно будет обновить разделы конфигурации `architect.build` и `architect.serve`.

Найдите ключ `architect.build` и в `builder` замените `@angular-devkit/build-angular:browser` на `@angular-builders/custom-webpack:browser`.

```diff-json
  "architect": {
    "build": {
-      "builder": "@angular-devkit/build-angular:browser"
+      "builder": "@angular-builders/custom-webpack:browser"
    }
  }
```

В параметрах `options` чуть ниже добавьте объект `customWebpackConfig` с `path` к конфигурации webpack:

```diff-json
  "architect": {
    "build": {
      "builder": "@angular-builders/custom-webpack:browser",
      "options": {
+       "customWebpackConfig": {
+         "path": "./webpack.config.js"
+       },
        "outputPath": "dist/angular",
      }
    }
  }
```

Теперь давайте сделаем что-то очень похожее в `architect.serve`:

```diff-json
  "architect": {
    "serve": {
-     "builder": "@angular-devkit/build-angular:dev-server",
+     "builder": "@angular-builders/custom-webpack:dev-server",
      "options": {
        "browserTarget": "angular:build",
+       "customWebpackConfig": {
+         "path": "./webpack.config.js"
+       }
      }
    }
  }
```

```preval configuration
purge: 
  - ./path/to/files 
```

---

```preval finish
scripts:
  - npm run start
```

