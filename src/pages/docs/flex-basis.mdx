---
title: "Основы флексов"
description: "Утилиты для управления начальным размером flex-элементов."
---

import utilities from 'utilities?plugin=flexBasis'
import { ArbitraryValues } from '@/components/ArbitraryValues'
import { BreakpointsAndMediaQueries } from '@/components/BreakpointsAndMediaQueries'
import { HoverFocusAndOtherStates } from '@/components/HoverFocusAndOtherStates'

export const classes = { utilities }

## Основы использования

### Настройка гибкой основы

Используйте утилиты `basis-*` для установки начального размера гибких элементов.

```html {{ example: true }}
<div class="flex flex-row space-x-4 font-mono text-white text-sm font-bold leading-6">
  <div class="basis-1/4 h-14 rounded-lg flex items-center justify-center bg-fuchsia-500 shadow-lg">01</div>
  <div class="basis-1/4 h-14 rounded-lg flex items-center justify-center bg-fuchsia-500 shadow-lg">02</div>
  <div class="basis-1/2 h-14 rounded-lg flex items-center justify-center bg-fuchsia-500 shadow-lg">03</div>
</div>
```

```html
<div class="flex flex-row">
  <div class="**basis-1/4**">01</div>
  <div class="**basis-1/4**">02</div>
  <div class="**basis-1/2**">03</div>
</div>
```

---

## <Heading ignore>Применяя условно</Heading>

### <Heading ignore>Наведение, фокус и другие состояния</Heading>

<HoverFocusAndOtherStates defaultClass="basis-1/3" featuredClass="basis-1/2" />

### <Heading ignore>Контрольные точки и медиа-запросы</Heading>

<BreakpointsAndMediaQueries featuredClass="basis-1/3">

```html
<div class="flex flex-row">
  <div class="basis-1/4 **md:basis-1/3**">01</div>
  <div class="basis-1/4 **md:basis-1/3**">02</div>
  <div class="basis-1/2 **md:basis-1/3**">03</div>
</div>
```

</BreakpointsAndMediaQueries>

---

## Использование пользовательских значений

### Настройка вашей темы

Базовая шкала гибкости по умолчанию представляет собой комбинацию [шкалы интервалов по умолчанию](/docs/customizing-spacing#default-spacing-scale), а также набора процентных значений.

Вы можете настроить масштаб интервалов, отредактировав `theme.spacing` или `theme.extend.spacing` в вашем файле `tailwind.config.js`.

```diff-js {{ filename: 'tailwind.config.js' }}
  module.exports = {
    theme: {
      extend: {
+       spacing: {
+         '112': '28rem',
+         '128': '32rem',
+       }
      }
    }
  }
```

Кроме того, вы можете настроить только базовый масштаб flex, отредактировав `theme.flexBasis` или `theme.extend.flexBasis` в вашем файле `tailwind.config.js`.

```diff-js {{ filename: 'tailwind.config.js' }}
  module.exports = {
    theme: {
      extend: {
+       flexBasis: {
+         '1/7': '14.2857143%',
+         '2/7': '28.5714286%',
+         '3/7': '42.8571429%',
+         '4/7': '57.1428571%',
+         '5/7': '71.4285714%',
+         '6/7': '85.7142857%',
+       }
      }
    }
  }
```

Дополнительные сведения о настройке темы по умолчанию смотрите в документации [настройка темы](/docs/theme#customizing-the-default-theme).

### Произвольные значения

<ArbitraryValues property="flex-basis" featuredClass="basis-[14.2857143%]" />
