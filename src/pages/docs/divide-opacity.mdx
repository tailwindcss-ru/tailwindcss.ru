---
title: "Разделение непрозрачности"
description: "Утилиты для управления непрозрачностью границ между элементами."
---

import plugin from 'tailwindcss/lib/plugins/divideOpacity'
import { Variants } from '@/components/Variants'
import { Disabling } from '@/components/Disabling'

export const classes = {
  plugin,
  transformSelector: (selector) => {
    return selector.split('>')[0].trim().replace(/^\./, '').replace(/\\/g, '')
  }
}

## Применение

Управляйте непрозрачностью границ между элементами с помощью утилит `divide-opacity-{amount}`.

```html purple
<template preview>
  <div class="divide-y-4 divide-black divide-opacity-25">
    <div class="text-center font-extrabold text-2xl text-purple-600 py-3">1</div>
    <div class="text-center font-extrabold text-2xl text-purple-600 py-3">2</div>
    <div class="text-center font-extrabold text-2xl text-purple-600 py-3">3</div>
  </div>
</template>

<div class="divide-y-4 **divide-black divide-opacity-25**">
  <div>1</div>
  <div>2</div>
  <div>3</div>
</div>
```

## Адаптивность

Чтобы контролировать непрозрачность границ между элементами в определенной контрольной точке, добавьте префикс `{screen}:` к любой существующей утилите разделения непрозрачности. Например, используйте `md:divide-opacity-50`, чтобы применить утилиту `divide-opacity-50` только при средних размерах экрана и выше.

```html
<div class="divide-y-2 divide-blue-500 divide-opacity-75 **md:divide-opacity-50**">
  <!-- ... -->
</div>
```

Для получения дополнительной информации о функциях адаптивного дизайна Tailwind ознакомьтесь с документацией [Адаптивного дизайна](/docs/responsive-design).

## Кастомизация

Чтобы настроить значения непрозрачности сразу для всех утилит, связанных с непрозрачностью, используйте раздел `opacity` в конфигурации темы `tailwind.config.js`:

```diff-js
  // tailwind.config.js
  module.exports = {
    theme: {
      extend: {
        opacity: {
+         '10': '0.1',
+         '20': '0.2',
+         '95': '0.95',
        }
      }
    }
  }
```

Если вы хотите настроить только утилиты непрозрачности разделения, используйте раздел `divideOpacity`:

```diff-js
  // tailwind.config.js
  module.exports = {
    theme: {
      extend: {
        divideOpacity: {
+         '10': '0.1',
+         '20': '0.2',
+         '95': '0.95',
        }
      }
    }
  }
```

Дополнительные сведения о настройке темы по умолчанию смотрите в [документации по настройке темы](/docs/theme#customizing-the-default-theme).

### Варианты

<Variants plugin="divideOpacity" />

### Отключение

<Disabling plugin="divideOpacity" />
