import { ApiTable } from "@/components/api-table.tsx";
import { ResponsiveDesign } from "@/components/content.tsx";

export const title = "isolation";
export const description =
  "Утилиты для управления тем, должен ли элемент явно создавать новый контекст наложения.";

<ApiTable
  rows={[
    ["isolate", "isolation: isolate;"],
    ["isolation-auto", "isolation: auto;"],
  ]}
/>

## Примеры

### Базовый пример

Используйте утилиты `isolate` и `isolation-auto` для управления тем, должен ли элемент явно создавать новый контекст наложения:

```html
<!-- [!code classes:isolate] -->
<div class="isolate ...">
  <!-- ... -->
</div>
```

### Адаптивный дизайн

<ResponsiveDesign property="isolation" defaultClass="isolate" featuredClass="isolation-auto" />
