import { ApiTable } from "@/components/api-table.tsx";
import { Example } from "@/components/example.tsx";
import { Figure } from "@/components/figure.tsx";
import { ResponsiveDesign } from "@/components/content.tsx";

export const title = "user-select";
export const description = "Утилиты для управления возможностью выбора текста пользователем в элементе.";

<ApiTable
  rows={[
    ["select-none", "user-select: none;"],
    ["select-text", "user-select: text;"],
    ["select-all", "user-select: all;"],
    ["select-auto", "user-select: auto;"],
  ]}
/>

## Примеры

### Отключение выбора текста

Используйте утилиту `select-none` для предотвращения выбора текста в элементе и его дочерних элементах:

<Figure hint="Попробуйте выделить текст, чтобы увидеть ожидаемое поведение">

<Example>
  {
    <div className="flex justify-center">
      <div className="dark:highlight-white/5 inline-flex rounded-lg bg-white px-4 py-3 text-center font-sans text-sm font-semibold text-gray-900 ring-1 ring-gray-900/5 select-none dark:bg-gray-800 dark:text-gray-200 dark:ring-0">
        Быстрая коричневая лиса перепрыгивает через ленивую собаку.
      </div>
    </div>
  }
</Example>

```html
<!-- [!code classes:select-none] -->
<div class="select-none ...">Быстрая коричневая лиса перепрыгивает через ленивую собаку.</div>
```

</Figure>

### Разрешение выбора текста

Используйте утилиту `select-text` для разрешения выбора текста в элементе и его дочерних элементах:

<Figure hint="Попробуйте выделить текст, чтобы увидеть ожидаемое поведение">

<Example>
  {
    <div className="flex justify-center">
      <div className="dark:highlight-white/5 inline-flex rounded-lg bg-white px-4 py-3 text-center font-sans text-sm font-semibold text-gray-900 ring-1 ring-gray-900/5 select-text dark:bg-gray-800 dark:text-gray-200 dark:ring-0">
        Быстрая коричневая лиса перепрыгивает через ленивую собаку.
      </div>
    </div>
  }
</Example>

```html
<!-- [!code classes:select-text] -->
<div class="select-text ...">Быстрая коричневая лиса перепрыгивает через ленивую собаку.</div>
```

</Figure>

### Выбор всего текста одним кликом

Используйте утилиту `select-all` для автоматического выбора всего текста в элементе при клике пользователя:

<Figure hint="Попробуйте кликнуть по тексту, чтобы увидеть ожидаемое поведение">

<Example>
  {
    <div className="flex justify-center">
      <div className="dark:highlight-white/5 inline-flex rounded-lg bg-white px-4 py-3 text-center font-sans text-sm font-semibold text-gray-900 ring-1 ring-gray-900/5 select-all dark:bg-gray-800 dark:text-gray-200 dark:ring-0">
        Быстрая коричневая лиса перепрыгивает через ленивую собаку.
      </div>
    </div>
  }
</Example>

```html
<!-- [!code classes:select-all] -->
<div class="select-all ...">Быстрая коричневая лиса перепрыгивает через ленивую собаку.</div>
```

</Figure>

### Использование автоматического поведения выбора

Используйте утилиту `select-auto` для использования стандартного поведения браузера для выбора текста:

<Figure hint="Попробуйте выделить текст, чтобы увидеть ожидаемое поведение">

<Example>
  {
    <div className="flex justify-center">
      <div className="dark:highlight-white/5 inline-flex rounded-lg bg-white px-4 py-3 text-center font-sans text-sm font-semibold text-gray-900 ring-1 ring-gray-900/5 select-auto dark:bg-gray-800 dark:text-gray-200 dark:ring-0">
        Быстрая коричневая лиса перепрыгивает через ленивую собаку.
      </div>
    </div>
  }
</Example>

```html
<!-- [!code classes:select-auto] -->
<div class="select-auto ...">Быстрая коричневая лиса перепрыгивает через ленивую собаку.</div>
```

</Figure>

### Адаптивный дизайн

<ResponsiveDesign property="user-select" defaultClass="select-none" featuredClass="select-all" />
