import { ApiTable } from "@/components/api-table.tsx";
import { Example } from "@/components/example.tsx";
import { Figure } from "@/components/figure.tsx";
import { ResponsiveDesign, UsingACustomValue } from "@/components/content.tsx";

export const title = "text-indent";
export const description = "Утилиты для управления количеством пустого пространства, отображаемого перед текстом в блоке.";

<ApiTable
  rows={[
    ["indent-<number>", "text-indent: calc(var(--spacing) * <number>)"],
    ["-indent-<number>", "text-indent: calc(var(--spacing) * -<number>)"],
    ["indent-px", "text-indent: 1px;"],
    ["-indent-px", "text-indent: -1px;"],
    ["indent-(<custom-property>)", "text-indent: var(<custom-property>);"],
    ["indent-[<value>]", "text-indent: <value>;"],
  ]}
/>

## Примеры

### Базовый пример

Используйте утилиты `indent-<number>` типа `indent-2` и `indent-8` для установки количества пустого пространства (отступа), которое отображается перед текстом в блоке:

<Figure>

<Example>
  {
    <p className="mx-auto max-w-sm indent-8 text-gray-900 dark:text-gray-200">
      Так я начал заходить в воду. Не буду врать вам, ребята, я был в ужасе. Но я продолжал идти, и когда я прошел мимо волн, 
      на меня снизошло странное спокойствие. Не знаю, было ли это божественное вмешательство или родство всех живых существ, 
      но говорю вам, Джерри, в тот момент я <em>был</em> морским биологом.
    </p>
  }
</Example>

```html
<!-- [!code classes:indent-8] -->
<p class="indent-8">Так я начал заходить в воду...</p>
```

</Figure>

### Использование отрицательных значений

Для использования отрицательного значения отступа текста добавьте дефис перед именем класса, чтобы преобразовать его в отрицательное значение:

<Figure>

<Example>
  {
    <p className="mx-auto max-w-sm -indent-8 text-gray-900 dark:text-gray-200">
      Так я начал заходить в воду. Не буду врать вам, ребята, я был в ужасе. Но я продолжал идти, и когда я прошел мимо волн, 
      на меня снизошло странное спокойствие. Не знаю, было ли это божественное вмешательство или родство всех живых существ, 
      но говорю вам, Джерри, в тот момент я <em>был</em> морским биологом.
    </p>
  }
</Example>

```html
<!-- [!code classes:-indent-8] -->
<p class="-indent-8">Так я начал заходить в воду...</p>
```

</Figure>

### Использование пользовательского значения

<UsingACustomValue element="p" utility="indent" name="text indentation" value="50%" variable="indentation" />

### Адаптивный дизайн

<ResponsiveDesign element="p" property="text-indent" defaultClass="indent-4" featuredClass="indent-8" />
